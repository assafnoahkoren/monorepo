version: '1.0'

services:
  postgres:
    image: postgres:15-alpine
    ports:
      - 5432:5432
    volumes:
      - ./volumes/postgres:/var/lib/postgresql/data
    env_file:
      - .env

  graphql-engine:
    image: hasura/graphql-engine:v2.40.3
    restart: always
    ports:
      - 8080:8080
    env_file:
      - .env
    depends_on:
      - postgres
